---
# CIFS path on bastion - override via extra_vars
bastion_image_path: "/mnt/cifs/bigip-images"

# Temporary staging dir on EE runner for image file (fetch from bastion, SCP to BIG-IP)
# Cleaned up after transfer completes
bigip_ee_staging_dir: "/runner/artifacts/images"
