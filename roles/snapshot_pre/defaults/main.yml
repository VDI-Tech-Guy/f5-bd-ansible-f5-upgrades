---
snapshot_output_dir: "{{ playbook_dir }}/../../../snapshots"
bigip_snapshot_save_ucs: true
bigip_ucs_path: "/var/local/ucs"
# Timestamp used in filenames - set once at play level for consistency
snapshot_timestamp: "{{ lookup('pipe', 'date +%Y%m%d_%H%M%S') }}"
