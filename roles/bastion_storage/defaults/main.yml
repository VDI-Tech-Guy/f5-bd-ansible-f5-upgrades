---
# CIFS backup root on bastion - override via extra_vars
bastion_backup_root: "/mnt/cifs/bigip-backups"

# EE runner staging dir - snapshot JSONs and UCS land here before bastion push
snapshot_output_dir: "/runner/artifacts/snapshots"

bigip_snapshot_save_ucs: true
